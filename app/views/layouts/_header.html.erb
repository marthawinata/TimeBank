<div data-role="header">
  <h1 text-align="centre">Timebank</h1>
</div><!-- /header -->
<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, :id => "flash_#{name}", :class => "ui-body ui-body-e" %>
<% end %>